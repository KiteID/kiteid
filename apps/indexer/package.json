{
  "name": "@kiteid/indexer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start --hostname 0.0.0.0 --port 42069",
    "codegen": "ponder codegen",
    "build": "ponder codegen",
    "typecheck": "ponder codegen && tsc --noEmit",
    "clean": "rimraf dist .ponder"
  },
  "dependencies": {
    "@sentry/node": "^10.52.0",
    "drizzle-orm": "0.45.2",
    "hono": "^4.12.18",
    "ponder": "^0.16.6",
    "viem": "^2.48.11"
  },
  "devDependencies": {
    "@kiteid/config-typescript": "workspace:*",
    "typescript": "^6.0.3"
  }
}
